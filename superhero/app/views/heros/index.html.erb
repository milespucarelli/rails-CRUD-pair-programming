<h1>Superheroes</h1>
  <ul>
    <% @heros.each do |hero| %>
    <li><%= link_to hero.name, hero_path(hero) %></li>
    <% end %>
  </ul>
<%= button_to 'Enter a New Superhero', new_hero_path, method: :get %>
